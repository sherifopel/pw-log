{
  "name": "pw-log",
  "version": "1.0.0",
  "description": "Colourful Playwright test logger — structured console output with sections, diffs, tables and pass/fail checks",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["playwright", "logger", "test", "chalk", "console"],
  "author": "Sherif Opel",
  "license": "MIT",
  "dependencies": {
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
